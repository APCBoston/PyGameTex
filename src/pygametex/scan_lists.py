#!/usr/bin/env python3

import sqlite3
from sqlite3 import error



def open_connection:
    """
    Open (or create) sqlite database, check if any of the tables are outdated,
    update if so.  Return a connection object.
    """


def close_connection:

